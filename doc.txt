Тут буде опис архітектури (сенсу описувати API немає, бо все є в постмані)

Опис в порядку обробки повідомлень:
app.ts - файл з ініціалізацією esxpress
також читає .env та за потреби викликає ініціалізацію таблиць sqlite

При надходженні повідомлення потрапляє в роутер (movies.routes або user.routes)
Там відбуваєтсья посилання на middleware валідації (валідація в свою чергу посилається на схеми zod)
Відповідальність валідації - перевірка наявності всіх потрібних полів та їх типів. УРазі помилки - генерація 400



